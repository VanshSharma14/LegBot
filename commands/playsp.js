const { Player } = require("discord-player")

module.exports = {
    name: "playsp",
    usage: "playsp",
    description: "play spotify in the current voice channel",
    action: (msg, args) => {
        
    }
}